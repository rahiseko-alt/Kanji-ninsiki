# 引き継ぎメモ

セッションをまたいで作業を再開するための記録。**新しいものを一番上に足す。**

- 1件の区切りは行頭の `## `。書式は `docs/agents/flow-map.md` の「引き継ぎメモ」に従う。
- 保存の上限と、会話開始時に読み込む件数は `.claude/hooks/session-start.sh` が持つ。
- 追記したら `sh .claude/hooks/handover-trim.sh` を実行して件数を整える。

---

## 2026-09-25 ホーム画面と4か国語を追加

**決めたこと**

- 利用者の依頼で、開いたときに出るホーム（説明・練習開始・5段階のカード・始める位置・言語）を追加。聞き取り（Q1〜Q4）は全問おすすめどおり。仕様書 rahiseko-alt/Kanji-ninsiki#25
- 表示言語を 英語・やさしい日本語・ベトナム語・ネパール語 の4つにした（ベトナム語・ネパール語は端末のフォントで表示）
- 初回のお知らせ画面はホームに統合して廃止（記録が端末だけに残る旨は記録の画面に表示）
- 利用者が示したデザイン画像どおりにホームを作り直した（和紙の色・墨の丸の「字」・3つの始める位置のカード・墨色のボタン・山の墨絵・下のタブ）。画像の一部（「字」と墨絵）は画像から切り出して使用。見出しの明朝体は未対応
- `/code-review` の指摘（訳語、初回画面の二度手間など）を修正済み。テスト96件・型の確認が通る

**次にやること**

- この提出を main に取り込む → 自動で公開される
- ベトナム語・ネパール語の訳を母語の方に確認してもらい、直す箇所を知らせてもらう
- 留学生での試用

**未解決の問題**

- ベトナム語・ネパール語の訳は母語話者の確認を受けていない【曖昧】
- 実機（スマホ）での動作は未確認

## 2026-09-25 始める位置を選べるようにし、くみたてる の出題範囲をそろえた

**決めたこと**

- 利用者の指摘（全員が「口」などから始まるのは意味がない、くみたてる だけ先の字が出る）を受け、両方に対応した
- 始める位置: 最初のお知らせと記録の画面で「はじめて（先頭）／すこし しっている（200字目）／よく しっている（600字目）」から選ぶ。変えると学習中の字だけが新しい位置の8字から始め直し、記録は残る
- くみたてる: 学習中の字で組み立てられる字が8字に満たなければ、学習中の字の先20字の中からだけ足す。1字も無ければ「ほかの練習をすると出てくる」と案内する
- テスト96件・型の確認が通る。この変更は rahiseko-alt/Kanji-ninsiki#24 に積んだ

**次にやること**

- #24 を main に取り込む → 自動で公開される
- 留学生での試用。200字目・600字目の区切りが合っているかも試用の結果で見直す

**未解決の問題**

- 始める位置の区切り（200・600）は仮の値で、根拠は無い
- 実機（スマホ）での動作は未確認

## 2026-09-25 Lv5 を公開し、くみたてる で同じ字ばかり出る不具合を修正

**決めたこと**

- Lv5（rahiseko-alt/Kanji-ninsiki#23）を main に取り込み、公開済み。Lv1〜Lv5 の全段階がそろった
- 利用者の指摘で、くみたてる が「見」ばかり出る不具合が判明（最初の学習中の8字で分かれる字が「見」だけだった）。分かれる字を出題順に8字以上そろえて出すよう修正した。ほかの段階の出し方は変えていない

**次にやること**

- この修正の提出を main に取り込む → 自動で公開される
- 留学生数名に1週間使ってもらい、記録画面の平均時間の移り変わりで効果を確かめる
- 今後段階や練習を足すなら、先に `/improve-codebase-architecture` で段階まわりの作りを整理する

**未解決の問題**

- 実機（スマホ）での動作は未確認。ADR-0004 の継承条件がアプリ本体に及ばないかは【曖昧】
- 練習の効果そのものは未検証（試用で確かめる）

## 2026-09-25 Lv4 を公開し、段階 Lv5「くみたてる」を実装（全5段階がそろう）

**決めたこと**

- Lv4（rahiseko-alt/Kanji-ninsiki#21）を main に取り込み、公開済み
- Lv5 の聞き取り（Q1〜Q5）は全問おすすめどおり。仕様書 #22（実装と同時に作成）
- 部品2つ（左右は横、上下は縦に並べる）を見せ、組み立てた字を 4→6→8 字から選ぶ。左右・上下にきれいに分かれ、部品がフォントにある 1388 字だけを出す
- 部品データは KanjiVG 由来で CC BY-SA 3.0 のまま同梱（ADR-0004）。フォントに部品の字形を追加（455KB）
- 実装と `/code-review` の指摘修正を済ませた。テスト87件・型の確認・組み立てが通る

**次にやること**

- Lv5 の提出（この枝の変更）を main に取り込む → 自動で公開される
- 留学生数名に1週間使ってもらい、記録画面の平均時間の移り変わりで効果を確かめる（最初に決めた完成の目安）
- 段階を足すたびに多くのファイルを直す作りになっている。今後段階や練習を足すなら、先に `/improve-codebase-architecture` で整理する

**未解決の問題**

- 段ごとの値を記録の同じ欄に持つ作り（互換のため）は残っている
- 実機（スマホ）での動作は未確認。ADR-0004 の継承条件がアプリ本体に及ばないかは【曖昧】（専門家に未確認）
- Lv5 の選択肢に、部品そのもの（例: 目＋八 の選択肢に 目）がまざることがある

## 2026-09-25 Lv3 を公開し、段階 Lv4「ちがう じを さがす」を実装

**決めたこと**

- Lv3（rahiseko-alt/Kanji-ninsiki#18）を main に取り込み、公開済み（Pages 設定済みなので、main への取り込みで自動公開される）
- Lv4 の聞き取り（Q1〜Q5）は全問おすすめどおり。仕様書 #19、作業指示書 #20
- 見本なしで、学習中の字1種類が並ぶ盤面から仲間はずれ（紛らわし字の上位3字から）を1つ押す。盤面 9→12→16 字、取り違えで並べた字と仲間はずれの両方を再出題
- 実装と `/code-review` の指摘修正を済ませた。テスト72件・型の確認・組み立てが通る

**次にやること**

- Lv4 の提出（この枝の変更）を main に取り込む → 自動で公開される
- 残る段階は Lv5（部品から組み立て。KanjiVG の部品データを使う）。進むなら `/grill-with-docs` から
- 段階を足すたびに複数ファイルを直す作りになっている。Lv5 の前に段階の一覧を1か所にまとめる整理（`/improve-codebase-architecture`）を検討する

**未解決の問題**

- 段ごとの値を記録の同じ欄に持つ作り（互換のため）は残っている
- 実機での動作は未確認。KanjiVG 由来データの CC BY-SA の扱いは【曖昧】のまま（Lv5 で KanjiVG の部品を表示する場合は判断が必要）

