# サーバーを持たない画面だけのサイトとして GitHub Pages で公開し、通信なしでも動かす

最初の版は、サーバー側の処理を持たない画面だけのサイトとして作り、GitHub Pages で無料公開する。ログインなし・記録は端末内（ADR-0002）なのでサーバーが不要で、運用費と壊れる部品を減らせるため。取り込むデータは数MB以内なので、一度開けば端末に保存し、電波のない場所でも練習できるようにする。先生用の画面や機械をまたいだ記録が必要になれば、サーバーの導入とあわせてこの決定を見直す。
